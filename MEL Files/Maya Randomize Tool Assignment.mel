select -all; 

global proc Randomize (float $amount, float $minX, float $maxX, float $minY, float $maxY, float $minZ, float $maxZ)
{
   string $objects[] = `ls - sl`;
   
   for($i=0; $i<size($objects); $i++)
   {
      for($j=0; $j<$amount; $j++)
      {
        duplicate $objects[$i]; 
         
        float $x = rand ($minX, $maxX);
        float $y = rand ($minY, $maxY);
        float $z = rand ($minZ, $maxZ); 
        
       
        duplicate ($x, $y, $z);
        
      }
       
       
   } 
   
   
   
} 

Randomize(10,0,10,0,10,0,100); //Now it's just not working at all. No duplicates and won't select anything. Ask for help tomorrow. 